import { JsonRpcWebSocket } from './websocket';
export declare class TransactionFactory {
    private address;
    private ws;
    constructor(address: string, ws: JsonRpcWebSocket);
    create(tx: UnpreparedTransaction): Promise<Transaction>;
    estimateGas(tx: Object): Promise<any>;
    nonce(): Promise<any>;
}
/**
 * Transaction that might need fields to be filled in, e.g., via estimateGas.
 */
declare type UnpreparedTransaction = {
    to?: string;
    value?: string;
    data?: string;
    nonce?: string;
    gasLimit?: string;
    gasPrice?: string;
    chainId?: number;
};
export declare type Transaction = {
    to?: string;
    value: string;
    data: string;
    nonce: string;
    gasLimit: string;
    gasPrice: string;
    chainId: number;
};
export {};
